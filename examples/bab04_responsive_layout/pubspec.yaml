name: bab04_responsive_layout
description: Contoh fokus Bab 4 - responsive layout
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: any

flutter:
  uses-material-design: true
